import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(22, 150, 172)',
      contrastText: '#FFF'
    },
    secondary: {
      main: 'rgb(22, 150, 172)',
    }
  },
});

export default theme;
